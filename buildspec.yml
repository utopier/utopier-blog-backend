version: 0.2

phases:
  build:
    commands:
      - echo Build Starting on `date`
      - sudo npm run build
  post_build:
    commands:
      - echo $(basename ./build/libs/*.jar)
      - pwd

cache:
  paths:
    - './node_modules/**/*'
